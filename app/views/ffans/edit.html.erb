<div>
	<%=form_with url: ff_ffcom_ffan_path(params[:ff_id], params[:ffcom_id], params[:id]), action:"update", method: :patch, local: true do |f|%>
	<%=f.text_area :body, value: "#{@answer.body}"%>
	<%=f.submit "Modifier"%>
	<%end%>
</div> 