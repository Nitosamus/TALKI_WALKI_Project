<h1>ProfessionalFormations#index</h1>

<table>
<% @professionalformations.each do |fp| %>
	<tr>
	<td><%= fp.id %></td>
 	<td><%= fp.objet %></td>
 	<td><%= fp.titre %></td>
 	<td><%= fp.lieu %></td>
 	<td><%= fp.contacte %></td>
 	<td><%=link_to "voir",professional_formation_path(fp.id)%></a></td>
 
</tr>

 <%end%>
</table>
